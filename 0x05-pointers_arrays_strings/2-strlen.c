#include"main.h"
/**
* _strlen -  a function that returns the length of a string
* @i: input
* @s: array
* Return: value of i
*/
int _strlen(char *s)
{
	int i;

	for (i = 0; *s != '\0'; ++i)
	return (i);
}
